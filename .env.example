# ContainerYard Environment Configuration

# Provider Type: MOCK | SIMULATION | REMOTE
PROVIDER=MOCK

# Server Port
PORT=5000

# Dashboard API Key (for securing endpoints if needed)
DASHBOARD_API_KEY=change-me

# Remote Agent Configuration (when using REMOTE provider)
REMOTE_AGENT_URL=https://your-agent:8088
REMOTE_AGENT_TOKEN=change-me

# Allowed Origins for CORS
ALLOWED_ORIGINS=http://localhost:5000,https://your-repl.replit.dev

# Optional: PostgreSQL Database (for saved views and user preferences)
# DATABASE_URL=postgresql://user:password@localhost:5432/containeryard

# Optional: Firebase Auth (for user authentication)
# FIREBASE_PROJECT_ID=
# FIREBASE_PRIVATE_KEY=
# FIREBASE_CLIENT_EMAIL=
